# Sandbox test defaults for pxe-pilot
# These are applied to all hosts unless overridden by a host-specific config

[global]
keyboard = "en-us"
country = "us"
timezone = "UTC"

# Plain-text password for sandbox testing only — never use in production
root_password = "pxe-pilot-test"

[network]
dns = "1.1.1.1"

[disk]
filesystem = "ext4"
